스택, 큐, 재귀 함수에 대한 이해 필요

1. 스택
LIFO -> 후입선출
파이썬에서는 별도의 라이브러리 필요없음. append()로 push, pop()으로 pop

2. 큐
FIFO -> 선입선출
from collections import deque
queue = deque() 로 생성
append(), popleft() 로 추가 및 삭제
list(queue) -> 리스트형으로 바뀜

3. 재귀 함수
자기 자신을 다시 호출하는 함수

4. DFS
깊이우선탐색
인접행렬(인접하지 않으면 무한대, 자기자신이면 0으로 표시) -> 2차원 리스트로 표현
인접리스트로 구현
재귀 함수 이용

5. BFS
너비우선탐색
인접리스트로 구현
while로 deque 이용